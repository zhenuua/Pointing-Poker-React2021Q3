import { SortingOptions } from '../../components/radio-group/types';

export const SearchPageConfig = {
  sortOptions: [SortingOptions.RELEVANCY, SortingOptions.POPULARITY, SortingOptions.PUBLISHED_AT],
};
