import React from 'react';
import './NoDataMessage.scss';

export const NoDataMessage: React.FC = () => {
  return <div className="no-data-message">no data was found or server issues - try again</div>;
};
