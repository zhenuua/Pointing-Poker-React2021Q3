import React from 'react';
import style from './main.module.scss';

const MainPage = (): JSX.Element => (
  <section className={style.main}>
    <h2 className={style.header}>MainPage</h2>
  </section>
);
export default MainPage;
